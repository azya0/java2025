// интерфейс, объявленный как interface Interface { ... },
// будет иметь пакетную область видимости,
// что ограничивает его использование только классами и интерфейсами в рамках одного пакета.


// Поскольку в объявлении не указаны модификаторы доступа (такие как public или private),
// компилятор Java подразумевает, что область видимости является пакетной.

interface Interface {
    
}
